export type UserType = {
    id: number
    firstName: string
    lastName: string
    email: string
    password: string
    role: number
}

export type InitialUserType = {
    firstName: string
    email: string
    role: number
}
