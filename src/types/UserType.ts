export type UserType = {
    id: number
    first_name: string
    last_name: string
    email: string
    password: string
    role: number
}
export type User2partType = {
    user_id: number
    cat_id: number
}
