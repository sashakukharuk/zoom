import {ActionsUserType} from "./ActionsUser";
import {UserStateType} from "./StateUser";

export const preloaderUser = (state: UserStateType, action: ActionsUserType) => {
    switch (action.type) {

        default:
            return state
    }
}
